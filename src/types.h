#ifndef TYPES_H
#define TYPES_H

#include <stdbool.h>
#include <stdint.h> // IWYU pragma: keep

typedef bool bit;
typedef uint8_t byte;
typedef byte word;
typedef uint16_t double_word;

#endif // TYPES_H
